# Contributing

Use macOS and Node.js 24 or newer. Install dependencies with `pnpm install`, then run `pnpm validate` before opening
a pull request. Changes to command behavior must update human help, JSON help, tests, and README examples together.
